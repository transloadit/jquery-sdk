{
  "name": "transloadit-jquery-sdk",
  "version": "3.0.0",
  "scripts": {
    "build": "npm run clean && npm run css && npm run browserify && npm run uglify && npm run gzip && npm run versionify",
    "clean": "rm -rf build/*",
    "css": "mkdir -p build/css/img && cp css/transloadit.css build/css/transloadit3-latest.css && cp -v css/img/* build/css/img/",
    "browserify": "browserify js/lib/*.js -t [ babelify ] -o build/jquery.transloadit3-latest.js",
    "dev": "npm run clean && npm run css && npm run browserify && npm run inject",
    "fix": "eslint . --fix",
    "gzip": "gzip --force --best < build/jquery.transloadit3-latest.js > build/jquery.transloadit3-latest.js.gz",
    "inject": "cp -v build/*.css ${WEBSITEDIR:-~/code/crm}/app/webroot/jquery-sdk/css/ && cp -v css/img/* ${WEBSITEDIR:-~/code/crm}/app/webroot/jquery-sdk/css/img/ && cp -v build/*.js* ${WEBSITEDIR:-~/code/crm}/app/webroot/jquery-sdk/js",
    "link": "ln -nfs $(pwd) ${WEBSITEDIR:-../content}/jquery-sdk",
    "lint": "eslint .",
    "release:major": "env SEMANTIC=major npm run release",
    "release:minor": "env SEMANTIC=minor npm run release",
    "release:patch": "env SEMANTIC=patch npm run release",
    "release": "npm version ${SEMANTIC:-patch} -m \"Release %s\" && git push --tags && git push && npm publish",
    "start:test:server": "node tests/server.js",
    "test": "npm run lint && tests/run.sh",
    "uglify": "uglifyjs --comments --stats --output=build/jquery.transloadit3-latest.js --source-map=build/jquery.transloadit3-latest.js.map -- build/jquery.transloadit3-latest.js",
    "versionify": "cd build && for file in *.*; do cp -v ${file} $(echo ${file} |sed \"s/latest/v$(npm run --silent version)/g\" ); done",
    "version": "node -e 'console.log(require(\"./package.json\").version)'"
  },
  "files": [
    "js",
    "css"
  ],
  "dependencies": {
    "is-online": "git://github.com/tim-kos/is-online",
    "tus-js-client": "1.4.2",
    "uuid": "2.0.2"
  },
  "devDependencies": {
    "babelify": "^7.3.0",
    "browserify": "13.0.0",
    "debug": "2.2.0",
    "eslint": "2.7.0",
    "eslint-config-standard": "5.1.0",
    "eslint-plugin-promise": "1.1.0",
    "eslint-plugin-standard": "1.3.2",
    "fakefile": "0.0.8",
    "uglify-js": "2.6.2"
  }
}
